####################################################################
					Job Portal Project
***************************************************************************************

1] Right click on Default.aspx and choose set as start page to make it startup page

2] When we use Eval fuction we can only use it if we have data binded to specified element like if we are using 
     GridView then we are changing value inside gridview using eval we need to bind data to gridview to do that	
     that data is not available to other elements 